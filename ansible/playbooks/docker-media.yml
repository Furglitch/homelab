---
- name: Apply docker-media configuration
  hosts: docker_media
  become: false  # running as root already; set to true if a sudo user is configured later
  roles:
    - baseline
    - docker_media

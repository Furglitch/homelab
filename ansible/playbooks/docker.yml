---
- name: Apply docker host configuration
  hosts: docker_hosts
  become: false  # running as root already; set to true if a sudo user is configured later
  roles:
    - baseline
    - docker
    - role: docker-media
      when: "'docker_media' in group_names"
    - role: docker-web
      when: "'docker_web' in group_names"

debug: false
uuid: "{{ pelican.wings_node_uuid }}"
token_id: "{{ pelican.wings_token_id }}"
token: "{{ pelican.wings_token }}"

api:
  host: 0.0.0.0
  port: {{ ports.wings_api }}
  ssl:
    enabled: false
    cert: /etc/letsencrypt/live/{{ ip_local }}/fullchain.pem
    key: /etc/letsencrypt/live/{{ ip_local }}/privkey.pem
  upload_limit: 100

system:
  data: /var/lib/pelican
  sftp:
    bind_address: 0.0.0.0
    bind_port: {{ ports.wings_sftp }}
    read_only: false

remote: "{{ pelican.app_url }}"
allowed_mounts: []
